---
interface Props {
  items: { id: string; label: string }[];
}
const { items } = Astro.props;
---
<nav class="page-toc" aria-label="このページの目次">
  <p>目次</p>
  <ol>{items.map((item) => <li><a href={`#${item.id}`}>{item.label}</a></li>)}</ol>
</nav>
